import { InsertHtmlDirective } from './insert-html.directive';

describe('InsertHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
